import * as React from 'react'
import styled from 'styled-components'

const HeaderButtonWrapper = styled.div`
  display: flex;
  margin-top: 30px;
`

const HeaderButton = () => {
  return <HeaderButtonWrapper>4 control</HeaderButtonWrapper>
}

export default HeaderButton
